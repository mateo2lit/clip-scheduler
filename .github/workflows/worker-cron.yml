name: Worker Cron

on:
  schedule:
    - cron: "* * * * *"
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Call worker endpoint
        run: |
          curl -sS "${{ secrets.WORKER_URL }}"
